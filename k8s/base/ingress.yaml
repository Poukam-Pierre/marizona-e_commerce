# ============================================================================
# Ingress Configuration
# ============================================================================
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shoppk-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/rate-limit: "100"
    nginx.ingress.kubernetes.io/rate-limit-window: "1m"
spec:
  tls:
    - hosts:
        - shoppk.com
        - www.shoppk.com
      secretName: shoppk-tls
    - hosts:
        - admin.shoppk.com
      secretName: admin-tls
    - hosts:
        - api.shoppk.com
      secretName: api-tls
  rules:
    # Storefront
    - host: shoppk.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: storefront
                port:
                  number: 3000
    - host: www.shoppk.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: storefront
                port:
                  number: 3000
    # Admin
    - host: admin.shoppk.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: admin
                port:
                  number: 3001
    # API
    - host: api.shoppk.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api
                port:
                  number: 3002
