# ============================================================================
# ConfigMap
# ============================================================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: shoppk-config
data:
  api-url: "https://api.shoppk.com/api/v1"
  admin-api-url: "https://api.shoppk.com/api/v1"
  storefront-url: "https://shoppk.com"
  admin-url: "https://admin.shoppk.com"
---
# ============================================================================
# Secrets Template (Use sealed-secrets or external-secrets in production)
# ============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: shoppk-secrets
type: Opaque
stringData:
  database-url: "postgresql://shoppk:CHANGE_ME@postgres:5432/shoppk?schema=public"
  redis-url: "redis://:CHANGE_ME@redis:6379"
  jwt-secret: "CHANGE_ME_TO_SECURE_SECRET"
  jwt-refresh-secret: "CHANGE_ME_TO_SECURE_SECRET"
  vapid-public-key: ""
  vapid-private-key: ""
