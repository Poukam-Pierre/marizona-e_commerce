# ============================================================================
# Kustomize Base
# ============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - api-deployment.yaml
  - storefront-deployment.yaml
  - admin-deployment.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: shoppk
  app.kubernetes.io/part-of: shoppk-platform
